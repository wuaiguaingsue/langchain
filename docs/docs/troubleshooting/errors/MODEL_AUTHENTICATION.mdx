# 模型认证

您的模型提供商拒绝您访问他们的服务。

## 故障排除

以下方法可能有助于解决此错误：

- 确认您的API密钥或其他凭证是正确的。
- 如果您依赖环境变量进行身份验证，请确认变量名称正确且已设置了值。
  - 请注意，环境变量也可以由`dotenv`等软件包设置。
  - 对于模型，您可以尝试明确传递`api_key`参数，以排除任何环境变量问题，如下所示：

```python
model = ChatOpenAI(api_key="您的密钥")
```

- 如果您使用代理或其他自定义端点，请确保您的自定义提供商不需要替代的认证方案。
