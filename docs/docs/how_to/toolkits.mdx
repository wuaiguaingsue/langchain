---
sidebar_position: 3
---
# 如何使用工具包

工具包是为特定任务设计的一组工具的集合。它们提供了方便的加载方法。

所有工具包都公开了一个 `get_tools` 方法，该方法返回一个工具列表。
因此，您可以执行以下操作：

```python
# 初始化工具包
toolkit = ExampleTookit(...)

# 获取工具列表
tools = toolkit.get_tools()

# 创建代理
agent = create_agent_method(llm, tools, prompt)
```
